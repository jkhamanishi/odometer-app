{
  "name": "odometer-app-form",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "exifr": "^7.1.3",
    "gh-pages": "^6.3.0",
    "typescript": "~6.0.2",
    "vite": "^8.2.2"
  }
}
